生成树协议(推测)
1.初始状态下,每个交换机均有一个唯一标识id,及各个端口传输代价const,各个端口序列mac的配置
2.生成树协议开始后,所有交换机均需要无条件认为自已是根网桥.
3.根网桥需要向自已的每一个接口发送dpdu报文(内容大致是说,我与根网桥相库,从我到根网桥需要花费f)
4.任一网桥收到此信息后,均需要将dpdu报文中指出的网桥id与自身认为的根桥进行比对,检查胜败,如果胜,则向收到此端口发送更正信息(指明自已是根网桥)
　　如果是败,则需要向自已的其它口发送报文,指明自已认为的根桥与网桥连接,花费是x(x是结合之前的f计算获得)
5.如果超过limit时间未收到反驳,则认为自已认为的根桥是对的.
6.每隔interval时间,非根桥均需要发新突破一次自认为对的根桥信息给相邻节点(相邻节点,参照４进行处理)





